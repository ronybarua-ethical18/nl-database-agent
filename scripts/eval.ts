/**
 * Milestone 5 — eval runner.
 *
 * Sends every question in evals/golden.json through the agent, runs the
 * reference SQL, compares result sets (order-insensitive), and prints an
 * accuracy scorecard. Exits non-zero if accuracy < EVAL_THRESHOLD.
 *
 * Run with: npm run eval
 */
import "dotenv/config";

// TODO(milestone 5): implement result-based grading
console.log("eval: not implemented yet");
