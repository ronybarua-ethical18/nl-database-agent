/**
 * Milestone 0 — seed script.
 *
 * Fills the demo e-commerce schema (customers, products, orders, order_items)
 * with a few thousand realistic rows via @faker-js/faker.
 *
 * Run with: npm run seed  (uses DATABASE_URL_OWNER from .env)
 */
import "dotenv/config";

// TODO(milestone 0): create tables + seed with faker
console.log("seed: not implemented yet");
